# Full typed table: orchestration handoff

Objective: finish building and validating `benchmark_full_20260910.typed` from all 2,315,652,132 rows in a fixed snapshot of `default.public_raw`. User explicitly selected all dates/environments, a fixed snapshot, the 13 tested columns, and current compute, and said Go. No PostgreSQL requests, service resizing, source writes, or live ingestion changes.

Work is active, not complete. The sequential fork-todos skill applies: the root orchestrates and integrates; one subagent performs each stage. Do not end after merely starting a background job. Continue until the table is published, independently validated and benchmarked, and reported.

Snapshot and loader stages completed. Read HANDOFF.md, LOADER_REPORT.md, and the active BULK_HANDOFF.md. Snapshot was cloned at 2026-09-10 23:14:47 UTC; all 17 part inventories/content hashes matched source. The isolated snapshot's merges are constrained so physical `_part`/`_part_offset` chunks stay stable. The frozen plan has 241 chunks; checkpoints and journal are authoritative.

Current owner is `/root/full_bulk`. It owns the full load, assembly and publication, but not final independent benchmarking. At this note, 210,692,581 rows / 22 chunks were verified, with no new steady-state errors. This count will become stale: read loader-state.json or agent progress. Current process was session 65266, log bulk-run-3.log; check current BULK_HANDOFF.md rather than assuming it remains unchanged.

INSERTs use two threads and a 4 GB query memory cap. A measured calibration established identical fingerprints at 25.49 seconds with two threads versus 13.93 seconds with four threads on 10M rows. Only fingerprint SELECTs now use four threads, within the existing 4-core/16-GiB service allocation; no resizing. Full-loader query-local transport permits this without changing shared pilot code or global settings. Latest complete chunks take roughly 72–77 seconds; several hours remain. Keep user updated at least every 60 seconds, preferring exact verified rows/current phase and concise messages.

Each chunk has strict tested conversion, exact row/null counts, and per-column plus full-row sum/XOR 128-bit fingerprints matched to raw conversion. Preserve errors, never silently coerce/drop/deduplicate or blindly replay INSERT. Per-partition compaction avoids background-pool saturation. Verify immutable snapshot inventory. Successful stages stay available for metadata-only assembly and recovery. Publication must recheck actual assembled payload multiset and expected total immediately before UUID-verified rename.

After `/root/full_bulk` completes, read its final handoff and spawn a sequential independent final-validation agent. It must review completeness/semantics/publication, verify all 241 checkpoint fingerprints and assembled totals, run appropriately bounded full-retention benchmarks across original and old/novel windows with repeated measurements and caches disabled, reuse saved PG evidence only, record source/snapshot/destination identities and active query/merge state, run local tests and privacy/diff checks, and write a self-contained full-build report with costs, timing, failures, storage and remaining limits. Do not let raw full-history correctness controls repeatedly scan the entire unindexed snapshot unnecessarily; reuse existing per-chunk verification and saved original hashes, and use bounded raw reference checks. It may need to wait for normal destination merges before claiming steady-state performance; report actual state. Keep original pilot and raw source untouched. User-approved deliverable is the full typed table, not rollups or live ingestion.

Existing unrelated search UI working-tree edits must remain untouched. User-facing final answer should report actual table name, full row count and scope, static snapshot date, validation and measured full-scale timings, link final report, and state any actual remaining limitations. Do not imply full-data equality is a cryptographic proof or logical referenced bytes equal distinct billed physical storage.
